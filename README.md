# snapyticket-final

Project installation and configuration

# Install pipenv by doing --  pip install pipenv 
# Move into the root of the project 
# and do -- pipenv install to install the packages in the Pip lock file
# activate virtual environment with  -- pipenv shell

# Now cd into the django project and make migrations and migrate
# Create a superuser
# run python manage.py runserver
